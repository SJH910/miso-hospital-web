# 🐛 프로젝트 전체 버그 점검 (2026-09-10, 2026-09-11 재점검)

> "더 버그 없나 확인해라" → "프로젝트 전체적으로 검토" 지시에 따라, 프론트(`frontend/`)/`was/`(Node)/`chatbot-service/`(Python) 세 영역을 각각 별도 에이전트로 병렬 전수 검토했다(각 파일 직접 읽고 grep/교차대조, **코드 수정은 안 함 — 리뷰 전용**). 아래는 그 결과.
>
> **[2026-09-11 재점검]** "이 프로젝트 파일들 다 검토하고 버그 리뷰 최신화" 지시로 같은 방식(3영역 병렬 에이전트, 리뷰 전용)으로 다시 전수 검토했다. 그 사이 커밋된 것(TOTP CSRF 수정, 관리자 드롭다운 메뉴, 팀원이 추가한 감사 로그 대시보드 `auditLog.js`/`audit-severity.js`/`audit_summary.py`/`admin-audit-dashboard.*` 등)까지 포함해서 다시 훑었다. 새로 찾은 항목은 각 제목 끝에 `(2026-09-11 발견)`으로 표시했고, 기존 항목은 상태를 재확인만 했다(별도 표시 없으면 그대로 미해결). 이번 재점검에서 나온 발견 중 일부(High 2건)는 직접 재현 검증까지 했다.

---

## 🔴 High

- [x] **chatbot-service `/chat`에 서비스 간 인증이 없음 — 수정 완료(2026-09-10)** (`chatbot-service/app.py`) — WAS(Node)와 챗봇(Python) 사이에 공유 시크릿/API 키 검증이 전혀 없었음. `ChatRequest.patient_id`를 body 그대로 신뢰. CORS(`app.py:25-31`)는 브라우저 cross-origin만 막을 뿐 서버 간 직접 호출은 못 막아서, 8000번 포트가 외부에 노출되면 임의 `patient_id`로 다른 환자의 예약/진료기록 존재여부·스캔문서 개수 조회 가능(`tools_db.py:70-153`)했던 IDOR.
  - **수정**: `was`/`chatbot-service` 양쪽 `.env`에 같은 `CHATBOT_SERVICE_KEY` 추가(공유 비밀키). `was/routes/chat.js`가 챗봇 호출 시 `X-Internal-Auth` 헤더로 이 값을 실어 보내고, `chatbot-service`는 `/chat` 진입 시 `hmac.compare_digest`(상수 시간 비교, 타이밍 사이드채널 방지)로 검증 후 불일치하면 `401`. 키가 `.env`에 없으면 `AUDIT_ENCRYPTION_KEY`와 같은 fail-fast 패턴으로 서비스 시작 자체를 막음(설정을 깜빡한 채 무방비로 뜨는 상황 방지).
  - **범위 밖으로 남겨둔 것**: 배포 시 8000번 포트를 아예 외부에 안 열리게 하는 네트워크/방화벽 수준 조치는 코드 수정이 아니라 배포 설정 영역이라 이번엔 안 함 — 배포 시점에 별도로 챙길 항목.
  - **검증**: 헤더 없이 직접 `curl` 호출 → `401`, 틀린 키로 호출 → `401`, WAS 로그인 후 정상 채팅 요청(`POST /api/chat`) → WAS가 올바른 키를 실어 보내 `200` 정상 응답 확인.
  - **[2026-09-11 재점검]** `chatbot-service/app.py`의 신규 라우트 `GET /audit-summary`도 같은 `verify_internal_caller`로 보호되는 것 확인 — 새 엔드포인트 추가 시에도 이 패턴이 지켜짐.
- [x] **PII 마스킹 우회 — 점(`.`)으로 구분한 주민번호/전화번호 — 수정 완료(2026-09-10)** (`chatbot-service/pii_masking.py`) — RRN·전화번호 정규식의 구분자 문자 클래스가 `[\s\-\~_]*`라 `.`이 빠져있었음. 검증: `"900101.1234567"`, `"010.1234.5678"` 둘 다 마스킹 안 되고 그대로 통과되던 것 확인. 이 문자 클래스가 `build_spaced_regex`/전화번호 패턴 조립부에 총 6번 중복 하드코딩돼 있어서(마침표 누락 자체가 이 중복 때문으로 보임) `SEPARATOR = r'[\s\-\~_.]'` 상수로 통합, 6곳 전부 재사용하도록 리팩터링. 버그 케이스(점 구분) 마스킹 정상화 + 기존 하이픈 케이스 회귀 없음 확인, 기존 테스트 스위트(`audit-agent/test_masking_secrets.py`) 8개 전부 통과.
- [x] **동기 블로킹 호출이 `async def` 핸들러 안에 그대로 있음 — 수정 완료(2026-09-11, `feature/ocr`)** (`chatbot-service/app.py` `chat_endpoint`) — pymysql DB 쿼리(`tools_db.py`), Gemini 호출(`3-1-llm.py`), `sqlite3.connect`가 스레드풀 오프로드 없이 이벤트루프에서 그대로 실행됨. `async def`는 FastAPI가 자동으로 스레딩 안 해주므로, 느린 요청 하나가 동시 요청 전체를 막음(운영 규모 병목).
  - **수정**: 확인해보니 `chat_endpoint` 안에 `await`가 단 하나도 없었음("async인 척하는 sync 함수") — `async def` → `def`로 한 단어만 변경. FastAPI는 sync `def` 라우트를 자동으로 별도 스레드풀에서 돌려주므로, 내부 코드는 전혀 안 건드리고 이벤트 루프 블로킹만 해소됨.
  - **검증**: 기능 회귀 없음(정상 질문 응답, 인증 401, 주제 제한 전부 정상). 동시성 개선 실측: 순차 3회 28.8초(요청당 평균 ~9.6초) vs 동시 3회(병렬 전송) 10.8초 — 수정 전이었다면 이벤트 루프가 막혀 순차와 비슷한 시간이 나왔을 것, 실제로 세 요청이 병렬 처리됨을 확인.
- [x] **`admin-totp-setup.js` TOTP 등록 시작 버튼에 CSRF 토큰 누락 — 수정 완료(2026-09-11, `feature/ocr`)** (`frontend/js/admin-totp-setup.js` `startEnrollButton`, `POST /api/totp/setup`) — 같은 파일의 나머지 두 버튼(확인/해제)엔 `X-CSRF-Token`이 붙어있는데 이것만 빠짐. 프로젝트 전체에서 CSRF 토큰이 빠진 유일한 상태변경 POST/PATCH/DELETE 호출(다른 파일 전수 대조 확인).
  - **실제 위험도**: `/setup`은 새 비밀키(secret+QR)를 응답으로 돌려주기만 할 뿐 이것만으로 계정 상태가 바뀌진 않음 — 진짜 활성화는 CSRF 보호가 걸려있는 `/verify-setup`에서 인증앱 코드까지 맞아야 확정되므로 즉각적인 악용 경로는 제한적이었으나, "모든 상태변경 요청은 CSRF 검증"이라는 프로젝트 전체 원칙에서 벗어난 유일한 구멍이라 수정.
  - **수정**: `was/routes/totp.js`의 `POST /setup`에 `verifyCsrfToken` 미들웨어 추가, `frontend/js/admin-totp-setup.js`의 해당 fetch에 `X-CSRF-Token` 헤더 추가 — 같은 파일의 나머지 두 요청과 동일한 패턴으로 통일.
  - **검증**: WAS 재시작 후 curl로 확인 — CSRF 토큰 없이 `POST /api/totp/setup` → `403`, 정상 토큰 포함 → `200`.
- [x] **`was/crypto-utils.js`의 `maskPii` — SSN/전화번호 마스킹이 구분자·형식 변형에 그대로 우회됨 — 수정 완료(2026-09-11 발견 및 수정)** (`was/crypto-utils.js:65-66` `SSN_PATTERN`/`PHONE_PATTERN`) — `SSN_PATTERN = /\d{6}-\d{7}/g`, `PHONE_PATTERN = /010-\d{4}-\d{4}/g`로 **하이픈 형식 하나만** 매칭. `chatbot-service/pii_masking.py`는 2026-09-10에 정확히 같은 클래스의 버그(구분자 문자 클래스 누락)를 `SEPARATOR` 상수로 통합해서 고쳤는데, **같은 프로젝트의 JS 쪽(`was/crypto-utils.js`)엔 그 수정이 전혀 반영되지 않았었음** — 같은 파일 63-64행 주석에 "규칙은 반드시 같이 업데이트할 것"이라고 적혀있는데도 실제로는 동기화가 안 됐었음.
  - **재현 확인(수정 전)**: `node -e`로 `maskPii()` 호출 — `"900101.1234567"`(점 구분 주민번호), `"010.1234.5678"`(점 구분 전화번호), `"011-234-5678"`(3자리 국번 구형 전화번호) 전부 마스킹 없이 원문 그대로 통과. `was/document-parsing.js`의 `maskFieldValue`가 이 함수를 그대로 재사용해서, OCR 스캔 결과 화면(`parsed_display_fields`)의 전화번호/연락처가 새는 것까지 실제 재현.
  - **수정**: Python `pii_masking.py`와 동일한 `SEPARATOR = "[\\s\\-~_.]"` 방식으로 `SSN_PATTERN`/`PHONE_PATTERN`을 캡처 그룹 기반으로 재구성. 전화번호 접두사도 `010` 하나만이 아니라 `0`+`1`+`[016789]`로 일반화해 011/016/017/018/019 구형 국번까지 커버. **같은 김에 Python 쪽의 별개 미해결 항목("구형 전화번호 3자리 국번 미마스킹")도 함께 고침** — 중간 구간을 4자리 고정에서 `(?:4자리|3자리)` 대체로 바꿔 011~019의 3자리 중간 구간(`011-234-5678`)도 마스킹되게 함(아래 Medium 항목 참고, 같이 해결됨).
  - **주의해서 고친 부분**: 정규식만 구분자 허용하게 바꾸고 마스킹 콜백(`m.split("-")`)을 그대로 두면, 매칭된 문자열에 점/공백이 섞였을 때 `split("-")`가 실패해 깨진 결과가 나옴 — 콜백을 `m.split("-")` 대신 정규식 캡처 그룹을 직접 쓰도록 함께 변경.
  - **검증**: `was/test-crypto-utils-secrets.js`(신규 케이스 3개: 점 구분 주민번호/전화번호, 구형 국번 3자리) + `chatbot-service/audit-agent/test_masking_secrets.py`(신규 케이스 3개, 동일 시나리오) 전부 통과, 기존 케이스 회귀 없음. WAS 재시작 후 `document-parsing.js`의 `parseDisplayFields()`로 재현 케이스 재확인 — 전화번호/연락처 정상 마스킹됨.
- [ ] **`was/.env`, `chatbot-service/.env`가 world-readable(0644)로 방치됨 (2026-09-11 발견)** — `ls -la`로 직접 확인: 둘 다 `-rw-r--r--`(0644). 이 안에 `GEMINI_API_KEY`, `AUDIT_ENCRYPTION_KEY`, `CHATBOT_SERVICE_KEY`(서비스 간 인증 공유 비밀키), DB 접속정보가 평문으로 들어있음. `secret.key`/`chatbot_logs.db`/`audit-logs/*`는 이미 `os.chmod(..., 0o600)`으로 하드닝됐는데(2026-09-10 수정 항목 참고), 정작 그 키들의 "원본 발급처"인 `.env` 자체는 어떤 코드에서도 chmod하지 않음.
  - **왜 심각한가**: 같은 머신의 다른 로컬 계정이 `.env`를 읽으면 (1) `CHATBOT_SERVICE_KEY`로 `/chat`·`/audit-summary`의 서비스 간 인증을 그대로 통과해 임의 `patient_id`로 IDOR 가능, (2) `AUDIT_ENCRYPTION_KEY`로 `secret.key` 없이도 `audit-logs/*.jsonl`을 직접 복호화해 감사로그 원문(PII 포함) 열람 가능 — 즉 `secret.key`/`chatbot_logs.db`에 적용한 권한 하드닝이 사실상 전부 무의미해짐(원본 키가 그대로 새고 있어서).
  - **근본 원인**: 시크릿 하드닝을 "파생 파일"에만 적용하고, 그 파생 파일들의 키를 실제로 담고 있는 원본(`.env`)은 빠뜨림. 배포 문서/스크립트 어디에도 `.env`용 `chmod 600` 안내가 없음.
- [ ] **spaCy 한국어 NER 모델이 설치돼 있지 않아 "문맥 없는 이름" PII 마스킹이 조용히 무력화됨 (2026-09-11 발견, fail-open)** (`chatbot-service/pii_masking.py:6-13, 222-244`) — 이름 마스킹은 두 겹: (1) "이름은/저는/나는..." 같은 트리거 접두사 뒤 이름만 잡는 정규식, (2) 트리거 없이 등장하는 이름을 잡는 spaCy 한국어 NER(`ko_core_news_sm`). 실제 환경에서 직접 로드 시도해 확인: `spacy`는 설치돼 있지만 `ko_core_news_sm` 모델이 없어 `OSError` → `except OSError: nlp = None`으로 **경고 로그 한 줄 없이** 조용히 (2)를 완전히 꺼버림. `requirements.txt`에도 `python -m spacy download ko_core_news_sm` 같은 모델 설치 단계가 없어서, 문서화된 절차대로 배포하면 항상 이 상태가 됨.
  - **실패 시나리오**: 사용자가 "김철수인데 예약 확인해줘"처럼 트리거 접두사 없이 이름을 말하면 전혀 마스킹되지 않은 채 masked_question이 외부 LLM(Gemini) API로 전송되고, 감사로그(`chatbot_logs.db.masked_text`, `audit_log.jsonl`)에도 평문 이름으로 남음 — "원문(PII)은 외부로 안 나가게 한다"는 `/chat`의 설계 목적(`app.py:121-122` 주석)을 조용히 우회.
  - **근본 원인**: 선택적 의존성(spaCy 모델)의 부재를 "해당 기능만 못 씀"으로 처리했지만, 그게 핵심 보안 기능(PII 마스킹)의 보장 범위 축소로 이어진다는 걸 아무도 알아챌 수 없게 조용히 처리함.

## 🟡 Medium

- [ ] **예약 슬롯 정원 체크 경쟁 조건** (`was/routes/reservations.js:82-94`) — 같은 시간대 COUNT와 INSERT가 트랜잭션/락 없이 분리된 별개 쿼리라, 거의 동시에 두 요청이 오면 둘 다 `count < 2`를 통과해 정원(2명)을 넘겨 예약될 수 있음.
- [ ] **`board.js` RBAC 불일치로 staff가 문의 상세를 못 봄** (`was/routes/board.js:108`) — `GET /:patientId`가 `board:read`를 요구하는데, `db/init.sql`상 `board:read`는 patient/admin에게만 있고 staff는 `board:reply`만 있음. 코드 주석("staff도 답변하려면 상세를 봐야 함")과 실제 권한 매핑이 안 맞아서, staff가 이 라우트를 치면 미들웨어 단계에서 그냥 403 — 목록(`GET /`)에서는 보이니 데이터 유출은 아니고 기능 불일치.
- [ ] **비동기 라우트에서 DB 에러 시 요청이 응답 없이 멈춤 — [2026-09-11 재점검: 범위 확장]** — Express 4는 async 핸들러의 reject를 에러 미들웨어로 자동 전달 안 함(`server.js`의 전역 `unhandledRejection` 로거는 응답을 안 보냄). try/catch 없는 라우트:
  - (기존) `records.js`(`POST /api/records`, `patient_id` 존재 검증도 없어서 FK 에러 나면 그대로 걸림), `reservations.js`(3개 라우트), `patients.js`, `accounts.js`, `totp.js`(`/status`, `/verify-setup`, `DELETE /`), `chat.js`(`GET /history`, `POST /`)
  - **(2026-09-11 발견, 기존 집계에서 빠졌던 것)**: `accounts.js`는 실제로는 `GET /`까지 포함해 **3개 라우트 전부** try/catch 없음(기존 문서엔 "2개"로만 기재돼 있었음). `documents.js`의 `GET /mine`/`GET /mine/:id`(같은 파일의 나머지 라우트는 다 있는데 이 둘만 빠짐), `holidays.js`의 `GET /`/`DELETE /:id`(`POST /`만 있음), `records.js`의 `GET /`(기존엔 POST만 언급됨)도 동일 패턴.
  - **(2026-09-11 발견, 새 파일)**: `board.js`는 4개 라우트(`GET /`, `POST /`, `POST /:id/answer`, `GET /:patientId`) **전부** try/catch 없음 — 이 파일 자체가 기존 09-10 점검 범위에 없었던 것으로 보임. `auditLog.js`(팀원이 최근 추가)도 `GET /`, `GET /summary` 둘 다 try/catch 없음 — 아래 별도 항목 참고.
- [ ] **`auditLog.js`의 `limit` 파라미터에 하한 검증이 없어 특정 값으로 요청이 그대로 멈춤 (2026-09-11 발견, 직접 재현)** (`was/routes/auditLog.js:14`) — `const limit = Math.min(Number(req.query.limit) || 50, 100);`은 **상한만** 클램프(`offset`은 `Math.max(...,0)`로 하한 처리가 있는데 `limit`만 빠짐). `mysql2.format()`으로 직접 확인: `LIMIT ? OFFSET ?`에 `[-5, 0]`을 바인딩하면 `"LIMIT -5 OFFSET 0"`이 그대로 만들어지고, MySQL은 음수 `LIMIT`을 문법 오류로 거부함. 바로 위 항목대로 이 라우트엔 try/catch가 없어서 **`GET /api/audit-log?limit=-1`을 호출하면 응답 없이 요청이 그대로 멈춤**. `audit:view` 권한이 있는 admin만 트리거 가능해 악용 실익은 낮지만, 실수로도 쉽게 유발되는 명확한 버그.
- [ ] **`chatbot-service/audit_summary.py`가 요청마다 전체 감사이력을 무제한으로 복호화·재스캔 (2026-09-11 발견)** (`chatbot-service/audit_summary.py:30-88`, `log_audit_tool.py`의 리더 함수들, `app.py:159-163`) — `build_audit_summary()`는 매 호출마다 `audit-logs/*.jsonl`(오늘자+모든 로테이션 백업) 전체, `chatbot_logs.db`의 `logs` 테이블 전체, MySQL `chat_messages` 전체를 복호화한 뒤 전부에 `mask_pii()`(정규식 다수 + spaCy NER)를 실행함. `MAX_FINDINGS_PER_SOURCE=20`은 **출력만** 자르지 계산량은 전혀 안 줄임 — 캐싱/날짜range 필터/페이지네이션이 없음. 감사로그가 수개월~수년 쌓인 운영 환경에서 관리자가 대시보드를 새로고침할 때마다(또는 `10/minute` 한도 내에서 반복 호출 시) 전체 이력 재계산이 일어나는 구조라, 데이터가 쌓일수록 점점 무거워지는 성능/DoS 문제.
- [ ] **`chatbot-service/audit_summary.py`의 소스별 예외 격리가 불완전 (2026-09-11 발견)** (`audit_summary.py:30-74`, `app.py:159-163`) — 리더 함수 호출(`reader()`)만 try/except로 감싸고, 그 다음 단계인 `evaluate_severity()`/`scan_for_pii()`(내부에서 `mask_pii()` 호출)는 예외 처리가 없음. `app.py`의 `audit_summary_endpoint`도 `build_audit_summary()` 전체를 감싸지 않음. 4개 소스 중 하나에서라도 처리 중 예외를 던지는 레코드가 있으면 `/audit-summary` 전체가 `500`으로 죽어서, 나머지 3개 소스는 멀쩡한데도 대시보드 전체가 깨짐. 같은 서비스의 `/chat`은 `mask_pii()` 호출을 명시적으로 fail-closed try/except(`app.py:124-128`)로 감싸 이 상황에 대비해뒀는데, 최근 추가된 `audit_summary.py`엔 같은 패턴이 빠짐.
- [ ] **프롬프트 인젝션 탐지 키워드가 하드코딩된 한국어 4개뿐이라 쉽게 우회됨 (2026-09-11 발견)** (`chatbot-service/audit-agent/masking.py:21`) — `malicious_keywords = ["지시사항 무시", "프롬프트 출력", "시스템 프롬프트", "이전 지시 무시"]`를 단순 부분 문자열 매칭. 이 플래그가 `risk_classification.py`에서 위험도를 CRITICAL로 승격시키고 `audit_decorator.py`의 실시간 경고를 트리거하는 유일한 근거인데, 영어 표현("ignore previous instructions")이나 어순/띄어쓰기 변형은 전혀 안 걸림. LLM 자체의 인젝션 내성과는 별개로(그건 시스템 지시문이 담당, 이번 리뷰 범위 밖), **감사/알림 기능**이 이 우회에 취약해서 실제 인젝션 시도가 있어도 관리자에게 안 보일 수 있음(`risk_level=low`로 조용히 기록).
- [ ] **`parse_datetime_kr`의 ISO 날짜 분기에 범위 검증이 빠져있음 (2026-09-11 발견)** (`chatbot-service/hospital_agent.py:76-80`) — 같은 함수의 `MONTH_DAY_PATTERN` 분기는 `try/except ValueError`로 존재하지 않는 날짜(2월 30일 등)를 안전하게 거르는데, ISO 분기(`datetime(int(y), int(mo), int(d), ...)`)엔 이 방어가 없음. "2026-13-40 25:99에 내과 예약해줘"처럼 정규식 형식만 만족하고 값이 무효인 입력이 오면 `ValueError`가 `run_agent` → `chat_endpoint`의 넓은 `except Exception`까지 전파돼 "죄송합니다, 현재 챗봇 서비스에 문제가 발생했습니다"라는 원인 불명확한 일반 오류로 나감(본래 의도된 "날짜 형식을 이해 못 했습니다" 안내 대신). 서비스 중단은 아니나 UX 저하.
- [ ] **여러 프론트 폼에서 "제출 중" 버튼 비활성화 가드 누락 — 중복 제출 가능 (2026-09-11 발견)** — `board.js:128-157`(문의 등록 `inquiryForm`), `admin-holidays.js:64-91`(휴진일 등록 `addHolidayButton`), `admin-accounts.js:117-152`(환자 등록 `registerButton`), `admin-reservations.js:68-80,96-115`(예약 승인/취소 `confirmBtn`/`cancelBtn`) — POST/PATCH 요청 중 버튼/폼을 비활성화하지 않아서, 빠른 연속 클릭(또는 Enter 두 번)으로 서버에 유니크 제약이 없는 데이터(문의글, 휴진일 등)가 중복 생성될 수 있음. 같은 프로젝트의 다른 버튼들(`admin.js`의 `scanButton`/`saveButton`, `admin-board.js`의 `saveBtn`, `admin-accounts.js`의 `changeRole`)은 `button.disabled = true` 가드가 있어서, **파일마다 방어 수준이 들쭉날쭉한 게 근본 원인**.
- [ ] **`chat-widget.js` 팝업 토글 — 빠른 연속 클릭 시 대화 이력이 중복 렌더링될 수 있음 (2026-09-11 발견)** (`frontend/js/chat-widget.js:6-13`) — 챗봇 버튼을 열기→닫기→열기로 빠르게 누르면, 첫 클릭의 `fetchChatHistory()`가 아직 응답 전(즉 `chatMessages.childElementCount`가 여전히 0)인 상태에서 세 번째 클릭이 같은 조건(`!isOpen && childElementCount === 0`)을 다시 통과해 **두 번째 fetch를 중복 발생**시킴 — 둘 다 도착하면 같은 대화 이력이 두 번 렌더링됨(중복 말풍선). "요청 진행 중"을 추적하는 플래그가 없어서 `childElementCount`만으로 "아직 안 불러옴"을 판단하는 게 원인 — `records.js`/`admin.js`에서 이미 두 차례 고쳤던 "늦게 도착한 응답이 상태를 덮어쓰는" 경쟁 조건의 변형(이번엔 요청 자체가 중복 발생).
- [ ] **`admin-audit-dashboard.js` — 수동 새로고침과 10초 자동 폴링이 겹치면 최신 데이터가 순간적으로 이전 데이터로 되돌아갈 수 있음 (2026-09-11 발견)** (`frontend/js/admin-audit-dashboard.js:215-217, 228-234`) — `refreshButton` 클릭과 `setInterval(loadDashboard, 10000)` 둘 다 "요청 중" 여부를 추적하지 않고, `loadDashboard()` 내부에서도 응답 도착 순서를 검증 안 함(`records.js`/`admin.js`가 쓰는 `currentDetailId`류 가드 없음). 자동 갱신과 겹치게 새로고침을 누르면 먼저 보낸 요청이 나중에 응답해서 화면이 최신→이전 데이터로 잠깐 되돌아갔다가 다음 10초 주기에 다시 맞음. 다음 갱신에서 자동 정정되므로 영구적 손상은 아니지만, 보안 감사 대시보드 특성상 일시적으로 오래된 위험도 수치를 보여줄 수 있어 Medium.
- [x] **PII 마스킹 우회 — 3자리 국번 구형 전화번호 — 수정 완료(2026-09-11)** (`chatbot-service/pii_masking.py:158-161`) — `phone_part2`가 4자리 고정이라 011/016/017/018/019의 3자리 국번 10자리 번호(예: `011-234-5678`)는 마스킹 정규식을 그냥 통과함(검증: 11자리 신형은 정상 마스킹, 10자리 구형은 원문 그대로 노출). **위 🔴 High의 `was/crypto-utils.js` 마스킹 우회 건을 고치면서 같이 해결** — 중간 구간을 `(?:4자리|3자리)` 대체로 바꿔 구형 국번도 마스킹되게 함. `chatbot-service/audit-agent/test_masking_secrets.py`에 회귀 테스트 추가, 통과 확인.
- [x] **`secret.key`/`chatbot_logs.db` 파일 권한 미설정 — 수정 완료(2026-09-10)** (`chatbot-service/app.py:39-67`) — 원문(마스킹 전) 채팅이 담긴 SQLite DB와 그걸 여는 Fernet 키가 `chmod` 없이 기본 OS 권한(0644)으로 생성되고 있었음(실측 확인). 감사로그 쪽(`audit_decorator.py:36,64`)은 과거 사고 이후 이미 0700/0600으로 굳혀놨는데 이쪽엔 같은 조치가 빠져있었음. 두 파일 생성/오픈 직후 `os.chmod(경로, 0o600)` 추가, 기존 파일도 소급 적용. 재시작 후 챗봇 정상 응답·로그 정상 기록 확인. 상세는 `report_merge_final.md`의 "감사 로그 암호화 키 이원화 정리" 항목 참고.
  - **[2026-09-11 재점검]** 위 🔴 High의 "`.env` world-readable" 항목 때문에, 이 조치로 하드닝한 파생 파일들의 원본 키(`AUDIT_ENCRYPTION_KEY`)가 여전히 `.env`를 통해 새고 있어 실효성이 줄어든 상태 — 두 항목을 같이 봐야 함.
- [x] **`records.js` 이미지 로딩 경쟁 조건 — 수정 완료(2026-09-10)** (`frontend/js/records.js` `loadDetail`) — `currentImageObjectUrl`이 호출 전체가 공유하는 변수 하나라, 문서를 빠르게 연속 클릭하면 나중에 도착하는 fetch가 이 변수를 덮어씀 — 화면엔 A 문서 이미지가 보이는데 내부적으로는 B의 blob URL을 "현재 것"으로 기억해서, 화면에 실제로 보이는 blob은 못 지우고(누수) 이미 안 보이는 blob만 지우는 상태가 됨. `currentDetailId`를 기록해두고 API 응답/이미지 fetch가 끝났을 때 여전히 그 문서를 보고 있는 게 맞는지 확인하는 가드 추가(`admin.js`에 새로 만든 문서 상세 패널에도 같은 가드를 처음부터 적용).

## 🟢 Low

- [ ] **로그아웃·TOTP setup 시작에 CSRF 토큰 검증 누락** (`was/routes/auth.js:281` `POST /api/logout`) — "모든 상태변경 요청에 CSRF" 원칙과 불일치. 로그아웃은 타 사이트가 강제로 로그아웃시킬 수 있는 정도로 피해 제한적. (TOTP setup 쪽은 2026-09-11에 수정 완료 — 위 🔴 High 참고.)
- [ ] **로그인 실패 추적 Map들이 무한정 쌓임** (`was/routes/auth.js:53-56`) — `attemptsByIp`/`failuresByUsername`/`knownIpsByAdminUsername`/`knownRegionsByAdminUsername`가 IP·계정명을 키로 하는데 바깥쪽 키 자체는 안 지워짐 — IP/계정명을 계속 바꿔가며 로그인 시도하면 메모리가 계속 늘어나는 완만한 DoS 벡터. (재시작 시 초기화되는 기존 한계와는 별개의 문제.)
- [ ] **DB 접속정보 하드코딩 fallback** (`chatbot-service/tools_db.py:14-16`, `log_audit_tool.py:49-53`, `log_retention_tool.py:35-39`) — env 변수 없으면 조용히 `vulnuser`/`vulnpass`로 폴백. 실패를 크게 내는 대신 짐작 가능한 자격증명으로 조용히 넘어감.
- [ ] **`log_retention_tool.py` 시간대 불일치** (`chatbot-service/log_retention_tool.py:115`) — cutoff는 `datetime.now(timezone.utc)` 기준인데 MySQL `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` 컬럼들은 서버 세션 시간대(보통 KST) 기준이라 약 9시간 오차 — 2년(730일) 보존 기준에선 반올림 오차 수준이라 실질 영향은 작음.
- [ ] **예약 진료과 문자열 검증 없음** (`chatbot-service/hospital_agent.py:55,62-63`) — `DEPARTMENT_PATTERN`이 `[가-힣]{2,6}과` 형태면 다 받아서 `reservations.department`(단순 VARCHAR, ENUM/FK 없음)에 그대로 저장 — 인젝션은 아니고(쿼리 파라미터화됨) 존재하지 않는 진료과명으로도 예약이 생길 수 있는 데이터 품질 문제. `report_merge_final.md`의 "진료과 자유 입력 여부" 결정 대기 항목과 같은 맥락.
- [ ] **`was/middleware/csrf.js`의 토큰 비교가 상수 시간 비교가 아님 (2026-09-11 발견)** (`was/middleware/csrf.js:7`) — `headerToken !== req.session.csrfToken`(`===` 계열). 기존에 지적된 `totp-utils.js`의 타이밍 사이드채널 문제와 같은 클래스. CSRF 토큰은 24바이트 랜덤값이라 네트워크 지터 때문에 실질 악용은 현실적으로 매우 어려움 — 참고 기록.
- [ ] **`was/config.js`의 `CHATBOT_SERVICE_KEY` 미설정 시 fail-fast 없음 (2026-09-11 발견)** (`was/config.js:43`) — `chatbotServiceKey: process.env.CHATBOT_SERVICE_KEY || ""`. chatbot-service 쪽은 이 키가 없으면 서비스 시작 자체를 막는 fail-fast인데, WAS 쪽은 비대칭적으로 조용히 빈 문자열로 기동됨 — 설정을 빠뜨려도 서버는 정상 기동하고, `chat.js`/`auditLog.js`의 챗봇 연동 기능만 매 요청 401을 받아 조용히 폴백(에러 메시지/데이터 제외)되는 상태로 남아 운영 중 원인 파악이 늦어질 수 있음.
- [ ] **`was/middleware/requirePermission.js`의 권한 캐시가 무기한 유지됨 (2026-09-11 발견)** (`was/middleware/requirePermission.js:7-23`) — `role_permissions`를 최초 1회만 조회해 프로세스 전역 캐시로 유지, 만료/재조회 로직 없음. 현재 이 매핑을 직접 수정하는 라우트는 없어 당장 악용 경로는 없지만, 운영 중 DB에서 직접 권한을 바꾸면(예: 긴급 권한 회수) 서버 재시작 전까지 반영 안 되는 구조적 리스크.
- [ ] **`was/routes/documents.js` — 이미지 저장 후 DB INSERT 실패 시 파일이 고아로 남음 (2026-09-11 발견)** (`was/routes/documents.js:100-113`) — 암호화된 이미지를 디스크에 먼저 쓰고 그다음 DB INSERT. INSERT 실패 시(요청 자체는 try/catch로 500 정상 응답) 이미 쓴 `.enc` 파일을 지우는 코드가 없어 고아 파일로 남음 — 파일시스템+DB 두 부수효과 사이에 원자성이 없는 구조적 문제.
- [ ] **`admin-audit-dashboard.js`의 `renderKpiRow`에서 `track.summary` 방어 누락 (2026-09-11 발견)** (`frontend/js/admin-audit-dashboard.js:43-49`) — `track.notable`은 `|| []`로 방어하는데 몇 줄 위 `track.summary.critical || 0`은 `track.summary` 자체가 없을 때 크래시 가능한 코드. 현재 백엔드(`was/routes/auditLog.js`, `chatbot-service/audit_summary.py`) 응답 계약상 `summary`가 비어있는 케이스는 실제로 발생하지 않는 것으로 보여 지금 당장 문제는 아니지만, 코드 자체의 방어 수준이 비대칭적이라 향후 응답 형태가 바뀌면 바로 터질 잠재 위험.
- [ ] **`chatbot-service/.env` 형식 오류로 감사 대시보드 요청마다 파싱 경고가 반복 출력됨 (2026-09-11 발견)** (`chatbot-service/.env`, `log_audit_tool.py:99`) — `.env`에 `#` 없이 적힌 설명용 텍스트 줄이 다수 있어 `python-dotenv could not parse statement` 경고가 뜸(현재 실제 키 값 로딩엔 지장 없음, 직접 검증 확인). `read_audit_jsonl()`이 `/audit-summary` 요청마다 `load_dotenv()`를 재호출하는 구조라 이 경고가 매 요청마다 반복됨(위 "매 요청 전체 재스캔" 항목과 결합해 로그 노이즈 가중). 향후 이 malformed 줄들 근처에 실제 설정을 추가하면 dotenv가 조용히 스킵할 위험도 있음.
- [ ] **`chatbot-service/app.py`의 `CORSMiddleware(allow_methods=["POST"])`인데 `/audit-summary`는 GET (2026-09-11 발견)** (`app.py:27-33, 159`) — 현재는 WAS가 서버 간(non-browser) 호출만 하므로 CORS가 이 경로에 실질 영향 없음(CORS는 브라우저만 강제). 설계 불일치로만 기록 — 향후 브라우저가 이 포트를 직접 호출하는 경로가 생기면 프리플라이트에서 막힘.
- [ ] **`chatbot-service/tools_db.py`의 `if not patient_id` 체크가 `patient_id=0`을 "미인증"으로 오인할 수 있음 — 확인 필요 (2026-09-11 발견)** (`tools_db.py:48,72,102,129`) — `not 0 == True`라 이론상 `patient_id=0`이면 정상 로그인 사용자도 거부됨. MySQL `AUTO_INCREMENT`가 보통 1부터 시작해 실제 발급 가능성은 낮지만, `db/init.sql` 스키마를 직접 대조하진 않아 100% 배제는 못 함.

## 점검 안 한 범위

`db/` 스키마 자체의 설계 재검토, `frontend`/`was`/`chatbot-service` 외 최상위 스크립트(`start.sh`/`stop.sh` 등), 배포/인프라 설정. 세 에이전트 모두 코드 수정은 하지 않았고 리뷰만 함 — 위 항목은 전부 아직 미해결 상태로 남아있다. LLM 자체의 프롬프트 인젝션 내성(시스템 지시문이 실제로 우회되는지)은 이번에도 코드 검토만으로는 확정 불가 — "확인 필요"로 남겨둠.
