# [최종 제출용] 보안 모니터링 대시보드

**접속**: https://miso-hospital.xyz/admin-audit-dashboard.html
**로그인 계정(admin)**: `admin` / `admin_test_123!` (`was/seed.js` 기준 — 실제 배포본은 변경되어 있을 수 있음)
**접근 조건**: `audit:view` 권한(admin 전용) — URL을 알아도 서버가 `requirePermission`으로 강제해 다른 역할은 접근 불가

> VM이 꺼져 있어 접속이 안 될 경우를 대비해, 아래 5개 항목 각각의 화면을 캡처해 이 파일에
> 이미지로 첨부해둘 것.

## 화면 구성

1. **위험도 요약 KPI** — 로그인 이상탐지 + 챗봇 도구 호출 위험도별(상/중/하) 건수
2. **주요 탐지 내역** — Critical/High만, WAS·챗봇 두 소스 통합 표시
3. **감사 로그 전체 이력** — 필터(카테고리/등급/계정/기간) + 페이지네이션, IP 옆에 차단 버튼(차단 여부에 따라 색 다름)
4. **PII 스캔 현황** — 챗봇 대화 원문 저장소 통합 스캔 결과 (마스킹 실패 / 원문 저장 건수)
5. **정적 점검 항목** — 코드/DB 스키마 구조적 결함 목록 (로그 이벤트 아님)

고위험 이벤트는 Discord로 실시간 알림이 오며, 알림의 링크를 클릭하면 이 대시보드의 해당 행으로 바로 이동·하이라이트된다.
