# 실제 배포를 위한 준비 사항 (단계별)

작성일: 2026-09-07

지금 이 프로젝트는 **개발자 로컬 컴퓨터 한 대**에서만 돌아가는 상태다 (Node 서버 하나, 파이썬 개발용 정적 서버 하나, 로컬 MySQL 하나). 실제로 환자·직원이 쓸 수 있게 배포하려면, 코드 기능 자체는 이미 되어 있어도 **인프라·보안·운영·법적 준비**가 따로 필요하다. 이 문서는 그 준비 사항을 단계별로 정리한다.

각 단계는 독립적이지 않고 서로 맞물려 있어서(예: HTTPS가 있어야 세션 쿠키의 `secure` 옵션을 켤 수 있음), 아래 순서를 큰 흐름으로 참고하되 실제로는 여러 단계를 병행하게 될 것이다.

---

## 우선순위 요약

| 순서 | 단계 | 왜 급한가 |
|---|---|---|
| 1 | 시크릿·환경변수 정리 | 지금 DB 계정("vulnuser"/"vulnpass")과 암호화 키가 전부 개발용 기본값 — 이대로 배포하면 안 됨 |
| 2 | 세션 저장소 교체 | 지금은 서버 껐다 켜면 전체 로그아웃되고, 서버를 2대 이상 띄우면 세션이 안 맞음 |
| 3 | HTTPS/정적 호스팅 전환 | 지금 프론트가 개발용 스크립트(파이썬)로 떠 있음 — 운영에 쓸 수 없음, 암호화 통신도 안 됨 |
| 4 | DB 백업/마이그레이션 전략 | 지금 스키마 변경 방식(`db/init.sql` 통째 재실행)은 기존 데이터를 지움 — 운영 DB에 그대로 쓰면 안 됨 |
| 5 | 법적/컴플라이언스 검토 | 주민등록번호·진료기록을 다루는 시스템이라 개인정보보호법 등 검토 없이는 배포 자체가 리스크 |

나머지(로깅/모니터링, CI/CD, 부하 테스트 등)는 위 5개보다는 급하지 않지만, 실제 운영을 안정적으로 하려면 결국 다 필요하다.

---

## 0단계 — 배포 전에 먼저 정해야 하는 것

- **배포 대상**: 클라우드(AWS/GCP/Azure/네이버클라우드 등)인지, 병원 자체 서버(온프레미스)인지. 이 프로젝트 폴더명이 "3-tier"인 것처럼 프론트/WAS/DB를 물리적으로 분리할지, 어느 수준까지 나눌지 결정 필요.
- **서버 OS**: 환자·직원이 쓰는 브라우저의 OS가 아니라, **WAS 서버를 실제로 돌릴 서버 컴퓨터의 운영체제**를 정하는 것. Node.js/MySQL 조합에서는 **Linux(권장: Ubuntu Server LTS 22.04/24.04)**가 사실상 표준이다 — 클라우드 기본 이미지 대부분이 Linux고, 관련 자료도 Linux 기준이 많고, 라이선스 비용도 없다. Windows Server는 라이선스 비용이 들고 Node.js 생태계 자료가 상대적으로 적어, 병원에 이미 Windows Server 인프라/담당 인력이 있는 게 아니라면 선택할 이유가 적다. (DB를 아래 2단계의 "매니지드 서비스"로 가면 DB 서버 OS는 클라우드가 알아서 관리하므로 신경 쓸 필요 없음 — 이 결정은 사실상 WAS 서버 한 대에만 해당됨. 참고로 지금 개발은 macOS에서 하고 있지만 Node.js는 OS를 가리지 않아 Linux 서버에 배포해도 코드 수정이 필요 없다.)
- **도메인**: 환자가 접속할 실제 주소(도메인) 확보. 지금은 로컬 IP(`localhost`, `172.16.x.x`)로만 접속 중.
- **Git 브랜치 전략**: `RBAC-Plan.md`에 이미 기록된 미정 사항 — 지금 작업 중인 원격 저장소에 정식 `main` 브랜치가 없다. 배포 파이프라인을 만들려면 "어느 브랜치가 배포 대상인지"부터 정해야 한다.
- **병원 내부 검토**: IT/보안 담당 부서나 병원 관리팀의 승인 절차가 있는지 확인 (`Plan.md` 끝에 "병원 관리팀과 미팅을 통해 요구사항 취합 필요"라는 메모가 이미 남아있다).

---

## 1단계 — 시크릿·환경변수 정리

`was/config.js`가 아래 값들을 환경변수로 받도록 이미 만들어져 있지만, 지금은 전부 로컬 개발용 기본값이 들어가 있다. 배포 전 실제 값으로 교체해야 한다.

| 환경변수 | 지금 상태(개발 기본값) | 배포 시 해야 할 일 |
|---|---|---|
| `DB_HOST`/`DB_PORT`/`DB_USER`/`DB_PASSWORD`/`DB_NAME` | `localhost`, `vulnuser`/`vulnpass`, `vulnapp` | 운영 DB 접속 정보로 교체. 계정 이름부터 개발용 티가 나므로 반드시 새로 발급 |
| `SESSION_SECRET` | 미설정 시 서버 시작할 때마다 랜덤 생성(재시작하면 기존 세션 전부 무효화) | 고정된 무작위 값 생성해서 안전하게 보관 |
| `RRN_ENCRYPTION_KEY` | 미설정 시 서버 시작할 때마다 랜덤 생성 — **이러면 재시작할 때마다 기존에 암호화 저장된 주민번호를 전부 복호화할 수 없게 됨** | 32바이트 hex 키를 한 번 생성해서 고정하고, 이 키 자체를 안전하게 백업(아래 9단계 참고) |
| `FRONTEND_ORIGIN` | 미설정 시 `localhost`/사설 IP 대역 + 5500번 포트를 자동 허용(로컬 개발 편의용) | 실제 배포 도메인으로 정확히 지정 — 이 값을 안 넣으면 개발용 자동 허용 로직이 그대로 운영에도 적용돼 CORS 정책이 지나치게 느슨해짐 |
| `USE_HTTPS` | 미설정(HTTP 취급) | `true`로 설정 — 세션 쿠키의 `secure` 플래그가 이 값으로 켜짐 (아래 3단계와 연결) |

**주의**: 이 값들을 `.env` 파일에 넣더라도 그 파일을 git에 커밋하면 안 된다(`.gitignore`에 이미 `.env`가 포함돼 있어 실수로 커밋되는 건 막혀 있음). 운영 환경에서는 서버에 파일로 두는 것보다 AWS Secrets Manager, GCP Secret Manager, HashiCorp Vault 같은 시크릿 관리 서비스에 저장하고 배포 시점에 주입하는 방식을 권장한다.

---

## 2단계 — 데이터베이스 준비

### DB를 어디서/어떻게 돌릴 것인가

"DB를 어떻게 배포할지"는 사실 아래 3가지 방식 중 하나를 고르는 문제다. 지금 `was/config.js`는 "MySQL이 어딘가에 이미 떠 있고 접속 정보(`DB_HOST` 등)만 넘겨주면 된다"는 전제로 짜여 있어서, 이 결정은 곧 "MySQL을 어디서 띄울지"를 정하는 것과 같다.

| 방식 | 설명 | 장단점 |
|---|---|---|
| A. WAS와 같은 서버에 DB 설치 | 서버 한 대에 Node 서버 + MySQL을 같이 설치 | 가장 싸고 간단하지만, 서버 하나가 죽으면 앱도 DB도 같이 죽는다. 백업·패치를 전부 직접 해야 함 |
| B. DB 전용 서버 분리(직접 관리) | WAS 서버와 DB 서버를 물리적으로 나눔 | 이 프로젝트 폴더명("3-tier")이 원래 의도한 구조. 다만 백업·패치·장애 대응을 여전히 직접 해야 함 |
| C. 매니지드 DB 서비스 (권장) | AWS RDS, GCP Cloud SQL, 네이버클라우드 Cloud DB for MySQL 등 | 백업·패치·이중화를 서비스가 대신 해줌. 비용은 더 들지만 운영 부담이 크게 줄어듦 — **병원 데이터처럼 민감한 데이터를 다룰 땐 이쪽을 강하게 권장** |

- **운영 DB 인스턴스 준비**: 위 C안(관리형 DB 서비스, AWS RDS, GCP Cloud SQL 등) 사용을 권장한다 — 자동 백업, 패치, 장애 복구를 서비스가 대신 해준다.
- **스키마 적용 방식 재검토**: 지금 `db/init.sql`은 맨 위에 `DROP TABLE IF EXISTS ...`로 시작해서, 실행할 때마다 기존 데이터를 전부 지우고 새로 만든다. 로컬 개발에는 편하지만 **운영 DB에 데이터가 쌓인 상태에서 이 스크립트를 그대로 실행하면 안 된다.** 이후 스키마 변경은 `ALTER TABLE` 기반의 마이그레이션 파일로 관리하는 도구(예: `node-pg-migrate` 유사한 MySQL용 도구, Prisma Migrate 등) 도입을 검토해야 한다.
- **최초 1회 스키마 적용**: 운영 DB가 비어있는 최초 배포 시점에 한해 `db/init.sql`을 실행하고, 그 이후로는 위 마이그레이션 방식으로 전환.
- **DB 계정 권한 최소화**: 지금 애플리케이션이 쓰는 DB 계정이 스키마 생성·삭제까지 다 할 수 있는 권한으로 보인다. 운영에서는 애플리케이션용 계정에 `SELECT`/`INSERT`/`UPDATE`/`DELETE`만 주고, 스키마 변경은 별도 관리자 계정으로 분리하는 걸 권장.
- **네트워크 격리**: DB가 외부 인터넷에서 직접 접속 가능하면 안 된다 — WAS 서버에서만 접속 가능하도록 보안 그룹/방화벽 설정 (3-tier 구조의 핵심).

---

## 3단계 — 세션 저장소 교체

지금 `was/server.js`의 `express-session` 설정에 별도 `store` 옵션이 없다 — 즉 **기본 메모리 저장소(MemoryStore)**를 쓰고 있다. 이건 공식적으로도 "프로덕션에 쓰면 안 됨"이라고 문서화된 방식이다.

- 서버 프로세스를 재시작하면 로그인 중이던 사용자 전부 로그아웃됨
- 메모리에 세션이 계속 쌓여서 장시간 운영 시 메모리 누수로 이어짐
- 서버를 2대 이상으로 늘리면(로드밸런싱), 사용자가 어느 서버에 붙느냐에 따라 로그인 상태가 왔다갔다함

**해야 할 일**: Redis 기반 세션 저장소(`connect-redis`)나 DB 기반 저장소(`express-mysql-session`) 중 하나를 도입해 `express-session`의 `store` 옵션으로 연결.

---

## 4단계 — HTTPS 적용 및 프론트엔드 정적 호스팅 전환

지금 프론트엔드는 `frontend/serve.py`(이번에 만든 개발용 스크립트) 또는 `python -m http.server`로 띄우고 있다. 둘 다 **로컬 개발 전용**이고 운영에는 절대 쓰면 안 된다 — TLS(HTTPS)를 지원하지 않고, 동시 접속 처리 성능이 낮고, 캐시/압축 같은 기본적인 웹서버 기능이 없다.

**해야 할 일**:
1. 정적 파일(`frontend/` 안의 HTML/CSS/JS)을 실제 정적 호스팅 서비스로 이전 — 예: Nginx로 직접 서빙, 또는 S3+CloudFront, Cloudflare Pages, Vercel 등.
2. 백엔드(WAS, `was/server.js`) 앞단에 리버스 프록시(Nginx 등)나 클라우드 로드밸런서를 두고 여기서 TLS를 종료.
3. 인증서 발급 — Let's Encrypt(무료, 자동 갱신) 또는 클라우드 제공 인증서.
4. 인증서 적용 후 `USE_HTTPS=true` 설정 (1단계에서 언급) — 세션 쿠키가 HTTPS에서만 전송되도록 강제됨.
5. `was/config.js`의 `FRONTEND_ORIGIN`을 새 정적 호스팅 주소(예: `https://miso-hospital.example.com`)로 정확히 지정.

---

## 5단계 — 보안 헤더·요청 제한 보강

- **보안 헤더 미들웨어(helmet.js) 미도입** — 현재 `was/package.json`에 helmet이 없다. `Content-Security-Policy`, `X-Frame-Options`, `X-Content-Type-Options` 같은 기본 보안 헤더가 응답에 없는 상태 — 추가 권장.
- **Rate limit이 인메모리 방식** — `express-rate-limit`이 기본적으로 서버 프로세스 메모리에 카운트를 저장한다. 서버를 여러 대로 늘리면 인스턴스마다 카운트가 따로 돌아서 "15분에 5회" 같은 제한이 사실상 무력화된다(인스턴스 수만큼 시도 가능해짐). Redis 기반 스토어로 교체 필요, 특히 로그인·OCR 라우트.
- **`npm audit` 취약점 5건 미해결** (`moderate 3, high 1, critical 1`) — `README.md`/`RBAC-Plan.md`에 이미 기록된 항목. 지금까지는 이 프로젝트 코드와 무관한 전이 의존성 문제로 판단해 남겨뒀지만, 배포 전에는 재검토·조치가 필요하다.
- **원래 이 프로젝트가 "취약점 실습용"에서 출발했다는 점** — 이미 8건의 알려진 취약점을 고친 이력이 있지만, 배포 전 별도의 외부 보안 점검(모의해킹 또는 최소 OWASP Top 10 체크리스트 재점검)을 받는 걸 권장한다.

---

## 6단계 — OCR(문서 스캔) 기능의 오프라인 대응

`tesseract.js`는 첫 실행 시 한글/영문 언어 데이터(약 7MB)를 인터넷에서 자동으로 내려받는다. 운영 서버가 병원 내부망처럼 외부 인터넷이 제한된 환경이라면:

1. 배포 전에 `.traineddata` 파일을 미리 받아서 `was/` 폴더에 함께 배포해야 한다 (`.gitignore`에 이미 이 파일들이 캐시로 분류되어 있음 — 배포 패키지에는 별도로 포함시켜야 함).
2. 최초 요청에서 다운로드가 걸리면 타임아웃이 날 수 있으므로, 서버 기동 스크립트에 "미리 한 번 실행해서 워밍업" 하는 단계를 추가하는 걸 권장.

---

## 7단계 — 프로세스 관리 (무중단 운영)

지금은 `node server.js`를 터미널에서 직접 실행하는 방식이다. 이 상태로는:
- 서버가 에러로 죽으면 자동으로 재시작되지 않는다
- 로그가 터미널에만 출력되고 파일로 관리되지 않는다
- 배포할 때마다 수동으로 서버를 껐다 켜야 한다(중단 시간 발생)

**해야 할 일**: 아래 중 하나 도입.
- **PM2** — 가장 간단한 Node.js 프로세스 매니저, 자동 재시작·로그 관리·무중단 재배포 지원
- **systemd 서비스 유닛** — 리눅스 서버에 직접 배포한다면 OS 레벨에서 관리
- **컨테이너화(Docker) + 오케스트레이션(ECS/Kubernetes 등)** — 지금 프로젝트에 `Dockerfile`/`docker-compose.yml`이 전혀 없어서, 이 방향으로 간다면 이것부터 새로 작성해야 한다.

---

## 8단계 — 로깅·모니터링·알림

- 현재 에러 처리는 `console.error`뿐이다(`was/server.js`의 에러 핸들러, 각 라우트의 catch 블록) — 운영에서는 로그가 흩어지고 유실되기 쉽다. 구조화된 로깅 라이브러리(winston, pino 등) + 중앙 로그 수집(CloudWatch Logs, ELK 스택 등) 도입 필요.
- 이번에 추가한 `audit_log` 테이블(로그인/계정변경 등 기록)은 **애플리케이션 감사 로그**이지, 서버 에러·성능을 보는 **운영 로그**와는 다른 목적이다. 둘 다 필요.
- **헬스체크 엔드포인트 없음** — 로드밸런서나 오케스트레이터가 서버가 살아있는지 확인할 방법이 없다. `GET /health` 같은 간단한 엔드포인트 추가 권장.
- 장애 발생 시 담당자에게 알림이 갈 채널(Slack, 이메일, 문자 등) 연결.

---

## 9단계 — 백업 및 재해 복구

- **DB 백업 자동화**: 주기(예: 매일), 보관 기간, 그리고 **복구가 실제로 되는지 리허설**까지 계획에 포함해야 한다(백업만 하고 복구 테스트를 안 해서 막상 필요할 때 복구가 안 되는 경우가 흔하다).
- **`RRN_ENCRYPTION_KEY` 백업이 특히 중요**: 이 키를 잃어버리면 DB에 암호화되어 저장된 주민번호를 전부 복호화할 수 없게 된다(DB 백업이 있어도 소용없음). 이 키는 시크릿 관리 서비스에 별도로, 여러 사람이 함부로 접근 못 하게 보관해야 한다.
- 백업 데이터 자체도 암호화 저장, 접근 권한 최소화(민감한 개인정보·의료정보이므로).

---

## 10단계 — 법적·컴플라이언스 검토

이 시스템은 주민등록번호와 진료기록을 다루므로, 기술적 준비만으로는 배포할 수 없다.

- **개인정보보호법** 준수 여부 — 특히 주민등록번호는 법적으로 처리가 엄격히 제한되는 고유식별정보라, 수집·저장·이용 목적이 법적 근거를 갖추고 있는지 법무 검토 필요.
- **의료 데이터 관련 추가 규제** 여부 확인 (의료법 등 — 진료기록을 다루는 시스템이 의료기기소프트웨어 등 별도 규제 대상인지도 확인 필요할 수 있음).
- **개인정보처리방침·이용약관 페이지**가 현재 프론트엔드에 없음 — 실제 서비스라면 필수.
- **개인정보 유출 사고 대응 절차**(신고 의무, 통지 절차 등) 사전 수립.
- 이 항목은 기술 팀만으로 결정할 수 없으므로 **법무/컴플라이언스 담당자 또는 병원 관리팀과의 협의가 선행**되어야 한다.

---

## 11단계 — 테스트/QA

- **자동화된 테스트가 현재 하나도 없다** (단위 테스트, 통합 테스트 모두 없음). 최소한 로그인, 권한 검사(RBAC), 결제/예약처럼 실수하면 안 되는 핵심 흐름에는 자동 테스트를 갖추는 걸 권장.
- **부하 테스트**: 예상 동시 접속자 수 대비 서버가 버티는지 확인. 특히 OCR은 CPU 비용이 큰 작업(`was/routes/ocr.js`의 `WORKER_POOL_SIZE`가 프로세스당 2개 고정)이라 동시에 여러 명이 스캔하면 병목이 될 수 있다.
- **UAT(실사용자 인수 테스트)**: 실제 병원 직원/환자 역할의 사람이 배포 전 화면을 직접 써보고 확인.

---

## 12단계 — 배포 파이프라인

- **CI/CD가 현재 없음** — GitHub Actions 등으로 "커밋 → 자동 테스트 → 빌드 → 배포"가 자동화되어 있지 않아 사람이 매번 수동으로 배포해야 한다.
- **브랜치 전략 확정** (0단계에서 언급) 이후, 그 브랜치에 병합되면 자동 배포되는 파이프라인 구성.
- **롤백 계획**: 배포 후 문제가 생겼을 때 이전 버전으로 빠르게 되돌리는 절차(이전 이미지로 재배포, DB 마이그레이션은 롤백 가능한 형태로 작성 등) 마련.

---

## 13단계 — 배포 이후

- 배포 직후 며칠간은 모니터링 대시보드로 트래픽·에러율을 특히 주의 깊게 관찰.
- 이용자 대상 서비스 오픈 공지, 문의/신고 채널 준비.
- `RBAC-Plan.md`/`README.md`/`변경사항-종합정리.md`에 이미 남아있는 미정 사항들(역할 세분화, 예약 시간대 중복 제한, 감사 로그 보존 정책 등)을 실제 운영 전에 마무리.
- 새로 생긴 기능(예약 관리, 답변 작성, 진료기록 조회, 계정 관리, 감사 로그 조회)의 **관리자/직원용 화면이 아직 없다** — 배포 시점까지 이 화면들이 준비되는지 별도 담당자와 일정 확인 필요.

---

## 참고 문서

- `README.md` — 실행 방법, 기능별 상세
- `RBAC-Plan.md` — 권한 설계 전체 이력, 미정 사항
- `변경사항-종합정리.md` — 최근 변경사항 요약 보고서
- `OCR.md` — OCR 기능 설계 이력
