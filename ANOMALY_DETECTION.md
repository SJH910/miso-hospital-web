# 비정상 요청 패턴 탐지 (반복 실패 / 이상 빈도)

## 담당 항목
백엔드 구현 항목 4번 — 비정상 요청 패턴 탐지

## 변경 파일

| 파일 | 구분 |
|---|---|
| `was/routes/auth.js` | 수정 |
| `was/totp-utils.js` | 신규 |
| `was/routes/totp.js` | 신규 |
| `was/server.js` | 수정 (totp 라우트 등록) |
| `was/package.json` | 수정 (`geoip-lite` 추가) |
| `db/init.sql` | 수정 (`patients.totp_secret` 컬럼 추가 — **재시딩 필요**) |
| `frontend/admin-totp-setup.html`, `frontend/js/admin-totp-setup.js` | 신규 |
| `frontend/login.html`, `frontend/js/login.js` | 수정 (TOTP 코드 입력창) |
| `frontend/admin.html`, `admin-holidays.html`, `admin-reservations.html`, `admin-board.html` | 수정 (nav 링크만 추가) |
| `frontend/js/qrcode.js` | 신규 (아래 "QR 코드 지원" 참고) |
| `frontend/js/nav.js` | 신규 (아래 "상단 nav 통일" 참고) |

## 탐지 기준

| 기준 | 조건 | 목적 |
|---|---|---|
| 반복 실패 | 같은 **계정**으로 5분 내 5회 이상 로그인 실패 (IP는 같든 다르든 무관 - 계정 이름 기준으로만 집계) | 같은 IP에서 벌이든 여러 IP를 바꿔가며 벌이든 상관없이 "이 계정이 집중적으로 공격받고 있다"는 것 자체를 탐지 |
| 이상 빈도 | 같은 **IP**에서 1분 내 10회 이상 로그인 시도 (계정은 같든 다르든 무관, 성공/실패 무관 - IP 기준으로만 집계) | 한 IP가 여러 계정을 순회하든 한 계정만 계속 벌이든 상관없이 "이 IP가 짧은 시간에 너무 많이 시도하고 있다"는 것 자체를 탐지 |
| 비정상적으로 긴 입력값 | 아이디 또는 비밀번호가 **200자 초과** | 정상적인 로그인 입력값이 이 길이를 넘을 일은 없음 - ReDoS, 파싱 취약점 등을 노리는 의도적 페이로드일 가능성 |
| 관리자 계정 반복 실패 (강화) | 같은 **admin 계정**으로 5분 내 **3회** 이상 실패 (일반 계정은 5회) | 관리자 계정은 뚫렸을 때 피해 범위가 훨씬 크므로, 같은 반복실패 기준이라도 임계값을 낮게 적용 |
| 관리자 계정 신규 IP 로그인 | admin 계정이 **과거에 로그인한 적 없는 IP**로 로그인 성공 | 관리자 계정 탈취 후 공격자가 실제 로그인에 성공하는 시나리오를 잡기 위함 (반복실패만으로는 "한 번에 성공"하는 경우를 못 잡음) |
| 관리자 계정 신규 지역 로그인 | admin 계정이 **과거에 로그인한 적 없는 지역**(국가-지역코드)에서 로그인 성공 | IP만 보면 "같은 지역인데 IP만 바뀌는 경우"(같은 통신사 재할당 등)도 매번 새 IP로 잡혀 오탐이 증가 - 지역까지 같이 보면 "IP는 바뀌었지만 익숙한 지역"과 "완전 미지의 지역"을 구분 가능 |
| **관리자 신규 위치 로그인 시 추가 인증 (TOTP)** | admin이 **TOTP를 등록해둔 상태**에서 신규 IP/지역으로 로그인 시, 비밀번호만으로는 로그인 미완성 - 인증 앱의 6자리 코드까지 맞아야 로그인 완료 | 위 두 항목(신규 IP/지역)은 "기록만" 하는 탐지인 반면, 이건 그 감지를 실제 로그인 차단으로 연결한 것 — 관리자 계정이 뚫려도 공격자가 실제 진입하지 못하게 막는 마지막 방어선 |
| SQL 인젝션 패턴 *(2026-09-14 추가)* | 아이디 또는 비밀번호에 흔한 SQLi 서명(`' OR '1'='1'`, `UNION SELECT`, `;DROP` 등)이 그대로 포함 | 쿼리는 파라미터화되어 있어 실행되진 않지만, 지금까지는 이 시도가 평범한 오타와 구분 없이 `login_fail`(low)로만 묻히고 있었음 — 성공/실패 무관하게 별도 이벤트로 남김 |
| 초과 크기 요청 *(2026-09-14 추가)* | 요청 본문이 100KB(`express.json()` 기본 제한) 초과 | 바로 위 "비정상적으로 긴 입력값"(200자 기준)보다 훨씬 큰 규모의 페이로드는 body-parser가 라우트 핸들러 진입 전에 막아버려서, 오히려 그 200자 기준 탐지조차 못 받고 그냥 에러로만 끝나던 사각지대 |

지역 판별은 `geoip-lite`(오프라인 IP-지역 매핑 패키지, 인터넷 호출 없음)를 사용한다. 사설 IP(로컬 테스트 환경 등)는 지역 추정이 안 되어 `null`을 반환하며, 이 경우 지역 기준 판단은 건너뛰고 IP 기준 판단만 적용된다.

두 기준(반복 실패/이상 빈도)은 서로 독립적으로 집계된다 (계정 기준 카운터와 IP 기준 카운터가 별도로 존재). 같은 시도라도 "계정 기준 임계값"과 "IP 기준 임계값"은 각각 다른 시점에 도달할 수 있어서, 동일 계정·동일 IP로 5번만 틀리면 반복 실패만 먼저 기록되고, 10번까지 틀리면 이상 빈도도 추가로 기록된다.

기존 `loginLimiter`(IP 기준 15분/5회 차단)와는 역할이 다름 — 그건 "더 이상 시도 못 하게 막는" 방어이고, 이 기능은 "이 패턴이 비정상이다"라고 **기록만** 하는 탐지. 로그인 자체를 막지는 않는다.

## 동작 방식

1. 로그인 요청이 들어오는 즉시(비밀번호 검증 전) 아이디/비밀번호 길이부터 확인 - 200자 초과면 즉시 기록 후 나머지 로직을 그대로 진행
2. DB에서 계정 조회 후, `role`이 `admin`이면 이후 로직에 "관리자 계정"이라는 플래그를 전달
3. 로그인 시도마다 IP별/계정별 타임스탬프를 메모리에 쌓아두고, 시간창 밖 기록은 제거(pruning)
4. 임계값에 **정확히 도달하는 순간**에만 1건 기록 (매 요청마다 중복 기록 방지). 관리자 계정은 반복실패 임계값이 낮게(3) 적용됨
5. 로그인 **성공** 시 해당 계정의 실패 카운트 초기화
6. **관리자 계정이면서 비밀번호까지 통과한 시점**에, 세션을 만들기 *전에* "이 IP/지역이 처음 보는 것인가"를 먼저 확인한다(`isNewAdminLocation` - 맵을 건드리지 않는 순수 조회):
   - TOTP를 등록 안 한 관리자라면: 신규 여부와 무관하게 로그인을 그대로 진행 (기존과 동일)
   - TOTP를 등록한 관리자이고 신규 위치가 맞으면: `totpCode`가 없으면 `requiresTotp: true` 응답만 주고 세션을 만들지 않음. 코드가 있으면 검증하고, 틀리면 401, 맞으면 계속 진행
7. 로그인이 최종 완료된 뒤(TOTP가 필요 없었거나 통과한 뒤)에만 `recordAdminLocation`을 호출해 IP/지역을 "알고 있음" 목록에 추가하고, 새로움이 있었으면 감사 로그도 남긴다 - 확인(6번)과 기록(7번)을 분리해서, 아직 로그인이 완료 안 된 상태에서 알고 있는 목록이 먼저 갱신되어버리는 것을 방지
8. 기존 `logAudit` 헬퍼를 그대로 재사용해 `audit_log` 테이블에 기록. 긴 입력값 탐지는 **실제 입력값을 로그에 남기지 않고 길이(숫자)만 기록** — 입력값 자체가 악성 페이로드일 수 있어 감사 로그에 그대로 옮겨적지 않는 편이 안전함

## TOTP 추가 인증 상세

### 왜 필요한가
"신규 IP/지역 로그인" 탐지는 원래 **기록만** 하는 기능이었다. 그런데 관리자 계정이 실제로 탈취되어 공격자가 다른 위치에서 정확한 비밀번호로 로그인에 성공하면, 기록은 남지만 **막지는 못한다.** 이 문제를 보완하기 위해, 관리자가 원하면(선택 사항) 신규 위치 로그인 시 추가 인증을 요구하도록 만들었다.

### 구현 방식: 순수 Node 내장 `crypto`만 사용
TOTP(RFC 6238)는 표준 HMAC 연산이라 `otplib` 같은 외부 패키지 없이 `was/totp-utils.js`에서 직접 구현했다. 이 프로젝트에서 `bcrypt`/`tesseract.js` 같은 네이티브 컴파일 패키지 때문에 여러 차례 설치 문제를 겪었던 걸 감안해, 필요 없는 의존성을 최대한 늘리지 않는 방향으로 결정.

### 등록(enrollment) 흐름 — `frontend/admin-totp-setup.html`
1. 관리자가 "등록 시작" 클릭 → `POST /api/totp/setup`이 새 비밀키를 생성해 반환 (이 시점엔 **아직 DB에 저장 안 함**)
2. 관리자가 그 비밀키를 인증 앱(구글 OTP 등)에 수동으로 입력 — 또는 아래 "QR 코드 지원" 참고
3. 앱이 보여주는 6자리 코드를 `POST /api/totp/verify-setup`으로 전송 → 서버가 그 코드를 검증해서 **맞을 때만** `patients.totp_secret`에 저장
4. 이렇게 "검증 후 저장" 순서로 만든 이유: 등록 과정에서 실수로 잘못된 비밀키가 그대로 저장되면, 이후 관리자 본인도 로그인을 못 하게 되는 사고를 방지하기 위함

### 로그인 시 검증 흐름 — `was/routes/auth.js`
1. 비밀번호 통과 + 관리자 + TOTP 등록됨 + 신규 위치 → `totpCode` 없이 온 요청은 `{success:false, requiresTotp:true}` 응답
2. 프론트(`login.js`)가 이 응답을 보고 코드 입력창을 노출, 사용자가 입력 후 재전송
3. 코드가 오면 `verifyTotpCode()`로 검증 (앞뒤 30초 오차까지 허용 - 인증 앱들의 일반적인 관례)
4. 통과해야만 세션 생성 진행

### 해제
분실 등으로 재등록이 필요하면 `DELETE /api/totp`로 본인이 직접 해제 가능 (관리자 화면에서 "TOTP 해제" 버튼).

## QR 코드 지원 (추가 작업)

등록 화면이 처음엔 비밀키를 텍스트로만 보여줘서 인증 앱에 "수동으로 키 입력"만 가능했다. 실사용 표준 흐름(QR 스캔)을 지원하기 위해 QR 렌더링을 추가했다.

- **라이브러리**: `qrcode-generator`(kazuhikoarase, MIT 라이선스) — 외부 의존 없는 순수 JS 단일 파일을 `frontend/js/qrcode.js`로 로컬에 벤더링. CDN이나 온라인 QR 생성 API는 사용하지 않음.
- **왜 온라인 QR API를 쓰면 안 되는가**: TOTP 비밀키가 QR에 그대로 인코딩되므로, 만약 온라인 API에 그 비밀키(`otpauthUri`)를 넘겨서 이미지를 받아오는 방식이었다면 비밀키가 제3자 서버로 그대로 전송되는 셈 — 이 기능 자체의 보안 목적(관리자 계정 보호)이 무너진다. 그래서 QR은 반드시 브라우저 안에서만(클라이언트 로컬) 렌더링한다.
- **동작**: `POST /api/totp/setup` 응답에 이미 포함돼 있던 `otpauthUri`(`was/routes/totp.js`가 생성)를 `frontend/js/admin-totp-setup.js`가 받아 `qrcode(0, 'M')`로 QR을 만들고 `createSvgTag()`로 SVG 마크업을 그려서 화면에 삽입. `otpauthUri`는 서버가 base32 비밀키로 직접 생성한 값(외부 입력 아님)이라 SVG를 그대로 삽입해도 안전.
- 비밀키 수동 입력 방식도 그대로 유지 — QR 스캔이 안 되는 환경(에뮬레이터, 인증 앱 미설치 등)을 위한 폴백.

## 상단 nav 통일 (추가 작업)

TOTP 설정 화면(`admin-totp-setup.html`)을 포함해 관리자용 화면 일부가 원래 다른 화면들과 다른 옛날 레이아웃(`board-container` 카드형)을 쓰고 있어서, 홈으로 돌아가는 링크·로그아웃 버튼이 아예 없거나 화면마다 이동 가능한 메뉴 목록이 제각각이었다. 로그인 후 화면 어디서나 동일한 상단 nav를 쓰도록 정리했다.

- **`frontend/js/nav.js`(신규)**: 역할별로 보여줄 메뉴 링크 목록을 한 곳에서만 관리하는 공용 모듈. `renderNavLinks(role)` 호출 한 줄로 각 페이지가 로그인한 계정의 역할에 맞는 전체 메뉴를 그린다. 새 메뉴가 추가될 때마다 여러 파일을 일일이 고칠 필요 없이 이 파일 하나만 수정하면 됨.
- **적용 페이지(11개)**: `index`, `board`, `reservation`, `records`, `view`, `admin`, `admin-holidays`, `admin-reservations`, `admin-board`, `admin-accounts`, `admin-totp-setup` — 전부 `<span id="navLinks">` 자리표시자로 교체하고 각자의 `loadUserInfo()`에서 `renderNavLinks(me.role)` 호출.
- **`admin.html`/`admin-holidays.html`/`admin-totp-setup.html` 헤더 복원**: 기존엔 `<h2>제목</h2>` + 부분 nav만 있고 홈 링크·로그아웃 버튼이 없었음 → 다른 화면들과 동일한 `site-header`(홈 로고 링크 + nav + 로그아웃 버튼)로 교체, `body class="landing-page"` 적용, 콘텐츠 카드(`.board-container`)가 자동 중앙정렬되도록 CSS 보정 추가.
- 실제 접근 제어는 여전히 서버(각 라우트의 `requirePermission`)가 담당 — nav에 어떤 링크가 보이고 안 보이는지는 UI 편의일 뿐, 보안 경계가 아님(기존 설계 원칙과 동일).

## 기록되는 이벤트

| action | detail 예시 |
|---|---|
| `login_anomaly_repeated_failure` | `{ username, ip, count: 5, threshold: 5 }` |
| `login_anomaly_high_frequency` | `{ ip, count: 10 }` |
| `login_anomaly_long_input` | `{ ip, usernameLength: 5, passwordLength: 350, maxAllowed: 200 }` |
| `login_anomaly_admin_repeated_failure` | `{ username, ip, count: 3, threshold: 3 }` |
| `login_anomaly_admin_new_ip` | `{ username, ip, knownIpCount: 1 }` |
| `login_anomaly_admin_new_location` | `{ username, ip, region: "KR-11", knownRegionCount: 1 }` |
| `totp_enrolled` | `{}` (등록 완료) |
| `totp_disabled` | `{}` (해제) |
| `totp_verify_success` | `{ username, ip }` (신규 위치 로그인 시 코드 검증 통과) |
| `totp_verify_fail` | `{ username, ip }` (코드 틀림) |
| `login_anomaly_sqli_pattern` *(2026-09-14 추가)* | `{ ip, field: "username" 또는 "password", matchedPattern, payloadSample }` — `payloadSample`은 다른 이벤트와 달리 마스킹 없이 원문(최대 200자)을 남김, 트리아지에 실제 공격 시도 모양이 보여야 하므로 |
| `oversized_request_payload` *(2026-09-14 추가)* | `{ ip, path, length, limit }` — 로그인뿐 아니라 모든 라우트 공통(전역 에러 핸들러) |

## 확인 방법

```sql
SELECT * FROM audit_log WHERE action LIKE 'login_anomaly%';
```

## 검증 완료

- 같은 계정 6회 연속 실패 → 5번째 시도에서 1건만 기록 (PASS)
- 같은 IP로 다른 계정 12회 시도 → 10번째 시도에서 1건만 기록 (PASS)
- 4회 실패 후 로그인 성공 → 카운터 초기화, 이후 재실패해도 즉시 재발동 유효 (PASS)
- 300자 비밀번호 입력 → 1건 기록 (PASS)
- 정상 길이 입력값 → 발동 없음 (PASS)
- admin 계정 3회 연속 실패 → 즉시 기록 (PASS, 일반 계정 임계값 5보다 낮음)
- 일반 계정 3회 실패 → 아직 기록 안 됨 (PASS, 임계값 5 미도달)
- 관리자 최초 로그인 → 발동 없음 (PASS, 비교 대상 기록이 없어도 "신규 IP"로 판단 안 함)
- 관리자가 신규 IP로 로그인 → 1건 기록 (PASS)
- 관리자가 기존에 알던 IP로 재로그인 → 발동 없음 (PASS)
- 같은 지역의 다른 IP로 로그인 → `new_ip`만 기록, `new_location`은 기록 안 됨 (PASS, IP와 지역을 독립적으로 판단)
- 완전히 다른 지역의 IP로 로그인 → `new_ip`와 `new_location` 둘 다 기록 (PASS)
- TOTP: 생성한 코드로 검증 → 통과 (PASS)
- TOTP: 틀린 코드(`000000`)로 검증 → 거부 (PASS)
- TOTP: 형식이 이상한 입력(`abcdef`)으로 검증 → 거부 (PASS)
- TOTP: 30초 전 코드도 검증 통과 → 시계 오차 허용 정상 동작 (PASS)
- TOTP: 90초 전 코드는 검증 거부 → 허용 범위 벗어남 정상적으로 막힘 (PASS)
- 로그인 통합 시나리오: 코드 없이 로그인 → `requiresTotp` 응답 / 틀린 코드 → 거부 / 맞는 코드 → 통과 (PASS)
- QR 코드: 실제 `otpauthUri`로 SVG 생성 확인 (PASS) — 실제 인증 앱(Google Authenticator) 스캔까지 사용자 확인 완료
- *(2026-09-14 추가)* SQL 인젝션 서명 로그인 시도(`' OR '1'='1' --`, `admin'--`) → high 정상 기록, 오타 비밀번호("patient1"+오타) → 여전히 low만 기록해 오탐 없음 확인 (PASS)
- *(2026-09-14 추가, 버그 발견 후 수정)* 대소문자만 바꿔가며(`admin`→`Admin`→`ADMIN`) 3연속 실패 → **수정 전**: MySQL은 대소문자를 구분 안 해서 셋 다 같은 관리자 계정으로 로그인되는데도, 반복실패 카운터는 대소문자별로 다른 키를 써서 이벤트가 전혀 안 뜸(우회 가능했음). **수정 후**: 3번째 시도에서 `login_anomaly_admin_repeated_failure` 정상 기록 (PASS)
- *(2026-09-14 추가, 버그 발견 후 수정)* 15만 자 요청 본문(100KB 초과) → **수정 전**: `express.json()` 크기 제한에 걸려 라우트 진입 전에 500 에러만 뜨고 아무 것도 기록 안 됨. **수정 후**: 413 응답 + `oversized_request_payload` 정상 기록 (PASS)

## 한계 / 다음 확장 지점

- 메모리 저장 방식이라 서버 재시작 시 카운트 초기화됨 (실습 규모라 문제 없으나, 이후 규모라면 Redis 등 외부 저장소 필요) — 관리자 신규 IP/지역 목록(`knownIpsByAdminUsername`, `knownRegionsByAdminUsername`)도 마찬가지라, 서버 재시작 직후 관리자가 이미 쓰던 IP로 로그인해도 "신규 IP"로 한 번 오탐될 수 있음
- 지금은 IP/지역 탐지 자체는 기록만 하고, **TOTP를 등록한 관리자에 한해서만** 실제 로그인 차단(추가 인증)까지 이어짐 — TOTP 미등록 관리자는 여전히 기록만 되고 로그인은 그대로 허용됨 (선택 사항으로 설계했기 때문)
- 일반 이상징후(반복실패/이상빈도/긴입력값) 자체는 여전히 기록만 하고 차단은 안 함 — 대시보드(항목 7) 쪽에서 이 이벤트들을 화면에 노출하면 자연스럽게 연결됨
- User-Agent나 VPN 여부까지는 아직 검토 안 됨 (요구사항에 언급된 "다른 IP·User-Agent·VPN" 중 IP·지역만 구현됨) — User-Agent 비교도 관리자 신규 위치 탐지의 동일한 패턴(과거 기록 Set과 비교)으로 확장 가능
- 200자라는 길이 기준은 임의로 정한 값이라, 실제 운영 중 사용자 데이터 분포를 보고 조정 필요
- 지역 판별(`geoip-lite`)은 사설 IP(로컬 개발 환경)에서는 동작하지 않음 - 실제 공인 IP 환경에서만 지역 기준 탐지가 의미있게 작동함
- TOTP 비밀키 등록/해제에 별도의 재인증(예: 현재 비밀번호 재입력) 절차가 없음 - 세션이 탈취된 상태라면 공격자가 TOTP를 몰래 해제하거나 자신의 기기로 재등록할 수 있는 이론적 여지가 있음 (실무에서는 민감한 보안 설정 변경 시 비밀번호 재확인을 추가하는 것이 일반적)
- QR은 클라이언트에서만 렌더링되고 서버 응답(`otpauthUri`)에 이미 포함돼 있던 값을 그대로 쓰는 것이라 백엔드 변경은 없음 — 화면 캡처가 막힌 인증 앱(1Password 등 보안 강화 앱)을 쓰는 경우 QR 자체는 문제없이 스캔되지만 그 앱 화면을 스크린샷으로 남기기는 어려울 수 있음(리포트용 캡처는 코드 입력→로그인 성공까지의 웹 화면으로 대체 가능)
- **[해결됨, 2026-09-14]** 관리자 계정 대소문자 변형(`admin`/`Admin`/`ADMIN`)으로 반복실패 카운터와 신규 IP/지역 목록(`knownIpsByAdminUsername`/`knownRegionsByAdminUsername`)이 전부 다른 키로 취급돼서 탐지가 우회될 수 있던 버그 — 단순 미탐을 넘어, 신규 위치 판정이 "기록 없음=새로움 아님"으로 오판해 TOTP 추가인증까지 건너뛸 수 있는 문제였음. 3개 Map 키를 소문자로 정규화(`normalizeUsernameKey`)해서 수정, 감사 로그 표시값은 원문 유지
- **[해결됨, 2026-09-14]** 100KB 초과 요청은 `express.json()`이 라우트 진입 전에 막아서 "비정상적으로 긴 입력값"(200자 기준) 탐지조차 못 받고 그냥 500 에러만 뜨던 미탐 — 전역 에러 핸들러에서 `entity.too.large`를 구분해 `oversized_request_payload`(medium) 기록 + 413 응답하도록 수정
