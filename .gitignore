node_modules/
.DS_Store
.env
*.log
.run/
__pycache__/
*.pyc
secret.key
chatbot_logs.db
.claude/settings.local.json

# tesseract.js가 첫 실행 시 자동으로 내려받는 OCR 언어 데이터 (was/routes/ocr.js) — 소스가 아닌 런타임 캐시
was/*.traineddata

# log_audit_tool.py가 실행할 때마다 생성하는 리포트 — 소스가 아닌 실행 결과물
chatbot-service/log_audit_report_*.csv

# 스캔 문서(처방전/진단서/영수증) 원본 이미지 — 암호화해서 저장하지만 민감한 의료 이미지라 git에는 안 올림
scanned-images/

# 로컬 전용 작업 기록 — git에 올리지 않기로 함(2026-09-10)
report_merge_final.md
LogDB_plan.md
