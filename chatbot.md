# chatbot-service 작업 기록

병원 챗봇 서비스(`chatbot-service/`) 관련 작업을 날짜별로 정리한 문서입니다.

## 2026-09-09
- 내부 URL/사설 IP 및 API 키(시크릿) 노출 탐지 기능 추가, 감사 로그 마스킹 통합, 해시체인 영속화 병합 (howl213)
- 스캔 문서 챗봇 도구, 계정 관리 화면 추가 (yysong1249)
- 감사 해시체인이 재시작/로테이션마다 초기화되던 버그 수정 (yysong1249)
- 챗봇 로그/키 파일 경로를 실행 위치(cwd)와 무관하게 고정 (yysong1249)
- 감사 로그 디렉터리/파일 권한을 소유자 전용으로 제한 (yysong1249)

## 2026-09-10
- 감사 기준 문서화 + `risk_level` 기반 위험도 리포트 도구 추가 (howl213)
- 주민번호/전화번호를 점(`.`)으로 구분한 경우 마스킹이 빠지던 우회 버그 수정 (yysong1249)
- `log_audit_tool.py` 추가: 4개 로그/감사 저장소 배치 리더, PII 스캔 리포트(3단계) (yysong1249)
- 감사 로그 보존 기간 정책(2년) + 수동 퍼지 도구 추가 (yysong1249)
- 위험 로그 등급 분류(상/중/하) + 아이디 마스킹, 예약 상태 시각화 개선 (Sunjung Hwang)
- `secret.key`/`chatbot_logs.db` 파일 권한을 소유자 전용(0600)으로 제한 (yysong1249)

## 2026-09-11
- `risk_level=high` 이벤트를 Discord 웹훅으로 실시간 알림 (howl213)
- PII 마스킹 우회(구분자 변형·구형 국번) 수정 - JS/Python 동시 반영 (yysong1249)
- `/chat` 엔드포인트의 동기 블로킹 호출이 이벤트 루프를 막던 문제 수정 (yysong1249)
- `chatbot-service` `/chat`에 서비스 간 인증(공유 비밀키) 추가 (yysong1249)
- 챗봇 일반 답변 경로에 병원 관련 질문만 답하도록 주제 제한 추가 (yysong1249)
- 감사 로그 대시보드 추가 (Sunjung Hwang)

## 2026-09-14 (오늘)
`chatbot-service/pii_masking.py`의 이름 마스킹 오탐/누락 문제를 진단하고 수정 (howl213):

- **오탐(과잉 마스킹) 수정**: "저는/나는 + N글자"만 보고 이름으로 단정하던 트리거 정규식에 한국 성씨
  화이트리스트(`KOREAN_SURNAMES`) 검증을 추가. "저는 아파요", "저는 미소병원 안내 챗봇입니다"
  같은 비-이름 문장이 더 이상 오탐 마스킹되지 않음.
- **2차 오탐 발견 및 수정**: 위 화이트리스트 자체가 "서울", "강남역", "김치찌개"처럼 성씨와 같은
  글자로 시작하는 지명·음식명을 새로 오탐하는 문제가 있어, 장소/도구 조사(에·에서·로 등) 뒤에서는
  이름 후보로 확장하지 않는 부정 조건을 추가.
- **누락 수정**: 트리거 단어 없이 등장하는 이름("홍길동 취소해줘")을 spaCy NER로 잡으려 했으나,
  기존 `ko_core_news_sm`(경량 모델)이 실제 이름을 PERSON이 아닌 다른 라벨(LC 등)로 잘못 분류하는
  경우가 많았음. `ko_core_news_md`(중간 크기 모델)로 교체해 라벨 정확도를 확인하고, 라벨(PERSON/PS)을
  다시 1차 신뢰 기준으로 사용하도록 되돌림. 성씨 화이트리스트는 라벨이 확정된 이름에서 조사(예:
  "김철수입니다"의 "입니다")를 잘라내는 보조 용도로만 사용.
- **환경**: 실제 서비스가 사용하는 Homebrew Python(`/opt/homebrew/opt/python@3.14`)에
  `ko_core_news_md`를 추가 설치 (기존 `ko_core_news_sm`은 폴백으로 유지).
- **알려진 한계(의도적으로 미해결)**:
  - "홍길동인데"처럼 트리거 없이 조사가 공백 없이 바로 붙는 경우, `ko_core_news_md`가 엔티티로도
    인식하지 못해 누락될 수 있음.
  - "나는 강남역 근처에 있어요"처럼 트리거 뒤에 조사 없이 지명이 오는 경우, 트리거 정규식 경로가
    spaCy를 거치지 않아 "홍길동 취소해줘"와 표면 구조가 같아 구분이 불가능함.
  - 두 한계 모두 지명/음식명 스톱리스트 추가, 또는 spaCy 형태소 태그(`jca`=장소격 조사 vs
    `jp`=서술격 조사) 기반으로 트리거 로직을 재설계하는 별도 작업이 필요.
